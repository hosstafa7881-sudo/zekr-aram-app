# پیشرفت دور هشتم — اپ اندرویدی «ذکرآرام»

وضعیت‌ها: ⬜ انجام‌نشده · 🟡 در حال انجام · ✅ انجام‌شده

## بخش الف — قابلیت‌های بومی

| # | مورد | وضعیت |
|---|---|---|
| ۱ | اشتراک‌گذاری بومی (`@capacitor/share`) — هر ۸ نقطه | ✅ انجام‌شده — نیاز به تست دستی کاربر روی گوشی |
| ۲ | ویبره‌ی بومی (`@capacitor/haptics`) — سه شدت قابل تفکیک | ✅ انجام‌شده — نیاز به تست دستی کاربر روی گوشی |
| ۳ | یادآوری روزانه و اعلان مناسبت‌ها (`@capacitor/local-notifications`) | ✅ انجام‌شده — نیاز به تست دستی کاربر روی گوشی |
| ۴ | ذخیره‌ی تصویر در گالری (`@capacitor/filesystem`) | ✅ انجام‌شده — نیاز به تست دستی کاربر روی گوشی |

## بخش ب — تصویر آماده‌ی استوری

| # | مورد | وضعیت |
|---|---|---|
| ۵ | باگ: به‌جای تصویر طراحی‌شده، اسکرین‌شات صفحه ذخیره می‌شود | ✅ انجام‌شده — علت واقعی پیدا و رفع شد |
| ۶ | بررسی کادر فروشگاه‌ها و QR با لینک آزمایشی (بعد از ۵) | ✅ انجام‌شده — ۹/۹ تست PASS |

## بخش پ — ظاهر اپ اندرویدی

| # | مورد | وضعیت |
|---|---|---|
| ۷ | حذف کامل صفحه‌ی اسپلش | ✅ انجام‌شده — نیاز به تست دستی کاربر روی گوشی |
| ۸ | بزرگ کردن آیکون برنامه (۲۵٪) | ✅ انجام‌شده — اندازه‌گیری شد: بدون بریدگی |
| ۹ | باگ روی‌هم‌افتادن toast و بنر در نوار بالا | ✅ انجام‌شده — با Playwright تست شد |

## بخش ت — داده‌ها

| # | مورد | وضعیت |
|---|---|---|
| ۱۰ | جایگزینی کامل جدول مناسبت‌ها (۵۰ قمری + ۱۰ شمسی) | ✅ انجام‌شده — ۸/۸ تست PASS |
| ۱۱ | باگ: نام ذکر ایام هفته در تاریخچه نمایش داده نمی‌شود | ✅ انجام‌شده — با Playwright تست شد |

## بخش ث — نسخه

| # | مورد | وضعیت |
|---|---|---|
| ۱۲ | شماره‌ی نسخه ← ۸ (و `versionCode`/`versionName` اندروید) | ✅ انجام‌شده — انجام شد |

## دستور نهایی

| # | مورد | وضعیت |
|---|---|---|
| ۱۳ | `build` و `type-check` تمیز | ✅ انجام‌شده |
| ۱۴ | ساخت APK قابل نصب + نوشتن مسیرش | ✅ انجام‌شده — GitHub Actions، artifact `zekraram-debug-apk` |
| ۱۵ | تست Playwright در ۳۶۰/۳۹۰/۴۱۲ + تبلت | ✅ انجام‌شده — ۱۹۶ بررسی، همه PASS |
| ۱۶ | گزارش تفکیکی ۱۲ موردی + فهرست پلاگین‌ها + متن‌های جدید | ✅ انجام‌شده |
| ۱۷ | به‌روزرسانی `MESSAGES-INVENTORY.md` و `SKILL.md` | ✅ انجام‌شده — انجام شد |
| ۱۸ | خلاصه‌ی غیرفنی + چک‌لیست تست دستی + Pull Request | ✅ انجام‌شده — PR #10 |

---

## نکته‌ی شاخه و PR

PR #10 (فاز اول Capacitor) هنوز merge نشده و کارهای این دور **روی همان پایه** بنا
می‌شوند (پلاگین‌های بومی بدون پروژه‌ی اندروید معنا ندارند). طبق قوانین شاخه‌ی این
نشست، همه‌ی کار روی `claude/peaceful-fermi-1ha0ac` می‌رود. در پایان به کاربر توضیح
داده می‌شود.

---

## نتیجه‌ی تست‌ها (دور هشتم)

| اسکریپت | نتیجه |
|---|---|
| `qa:layout` | ۵۰/۵۰ |
| `qa:flows` | ۲۵/۲۵ |
| `qa:flows2` | ۳۹/۳۹ |
| `qa:occasions` | ۱۴/۱۴ |
| `qa:misc` | ۹/۹ |
| `qa:v7` | ۲۷/۲۷ |
| `qa:v8` (تازه) | ۱۵/۱۵ |
| `qa:occasion-dates` (تازه) | ۸/۸ |
| `qa:storelinks` | ۹/۹ |
| **مجموع** | **۱۹۶ بررسی، همه PASS** |

## آنچه در سندباکس قابل تأیید نبود

موارد ۱، ۲، ۳، ۴، ۵ (رفتار روی گوشی)، ۷ و ۸ همگی بومی‌اند و فقط با APK روی
گوشی واقعی قابل تأیید‌اند. یک چیز مهم **قابل تأیید شد**: پلاگین بومی
`SaveImagePlugin.java` روی GitHub Actions **کامپایل می‌شود** (بیلد سبز)، پس
حداقل خطای ساخت ندارد.

---

## اصلاحات بعد از گزارش کاربر (۲۴ شهریور)

| مورد | وضعیت |
|---|---|
| کلید امضای ثابت — بدون آن هر نسخه‌ی جدید نیاز به پاک کردن اپ و از دست رفتن تاریخچه داشت | ✅ انجام‌شده |
| فونت فارسی داخل برنامه (قبلاً از سرور گوگل گرفته می‌شد؛ اپ آفلاین + انتشار در ایران) | ✅ انجام‌شده |
| **باگ بحرانی: پشتیبان‌گیری هیچ فایلی ذخیره نمی‌کرد ولی «با موفقیت دانلود شد» می‌گفت** | ✅ انجام‌شده |
| کپی کد تخفیف بدون fallback برای WebViewهای قدیمی | ✅ انجام‌شده |

### چرا باگ پشتیبان‌گیری از قلم افتاده بود

مورد ۴ این دور همین باگ را برای **عکس‌ها** رفع کرد، ولی جست‌وجو فقط حول تصاویر
انجام شده بود و مسیر دانلود **فایل پشتیبان** — که دقیقاً همان `<a download>` و
همان پیام موفقیت بی‌قید‌وشرط را داشت — دیده نشد. این جدی‌ترین نمونه‌ی همان باگ
بود، چون پشتیبان تنها محافظ تاریخچه‌ی کاربر است.

درس ثبت‌شده در `SKILL.md`: وقتی یک باگ از یک الگو پیدا شد، **کل برنامه** باید
برای همان الگو جست‌وجو شود، نه فقط جایی که گزارش شده.

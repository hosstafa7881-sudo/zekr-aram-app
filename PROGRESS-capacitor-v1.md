# پیشرفت فاز اول Capacitor (تبدیل به اپ اندرویدی) — «ذکرآرام»

وضعیت‌ها: ⬜ انجام‌نشده · 🟡 در حال انجام / منتظر کاربر · ✅ انجام‌شده

## موارد پرامپت

| # | مورد | وضعیت |
|---|---|---|
| ۱ | نصب Capacitor (`core`/`cli`/`android`)، `cap init`، `cap add android` | ⬜ انجام‌نشده |
| ۲ | آیکون برنامه (تولید همه‌ی سایزها + adaptive + legacy) و پیش‌نمایش سه ماسک | ⬜ انجام‌نشده |
| ۳ | اسپلش‌اسکرین (`@capacitor/splash-screen`) با پس‌زمینه‌ی پالت پیش‌فرض | ⬜ انجام‌نشده |
| ۴ | تنظیمات `capacitor.config.ts` | ⬜ انجام‌نشده |
| ۵ | بیلد و سینک اولیه (`npm run build` + `cap sync android` + تلاش برای APK دیباگ) | ⬜ انجام‌نشده |
| ۶ | `versionCode` / `versionName` اندروید و توضیح تفاوتشان با «شماره‌ی نسخه»ی داخل اپ | ⬜ انجام‌نشده |

## دستور نهایی

| # | مورد | وضعیت |
|---|---|---|
| ۷ | اطمینان از این‌که هیچ متن/رنگ/منطقی در وب‌اپ تغییر نکرده | ⬜ انجام‌نشده |
| ۸ | گزارش تفکیکی (اسکرین‌شات ماسک‌ها، اسپلش، نتیجه‌ی بیلد، توضیح versionCode) | ⬜ انجام‌نشده |
| ۹ | خلاصه‌ی غیرفنی + قدم بعدی کاربر | ⬜ انجام‌نشده |
| ۱۰ | ساخت Pull Request جدید | ⬜ انجام‌نشده |

## ابهام‌هایی که باید از کاربر پرسیده شود

| موضوع | وضعیت |
|---|---|
| فایل واقعی تصویر آیکون (ضمیمه‌ی پیام در محیط اجرا موجود نیست) | ⬜ منتظر پاسخ |
| تأیید نهایی `com.zekraram.app` به‌عنوان App ID (بعد از انتشار قابل تغییر نیست) | ⬜ منتظر پاسخ |

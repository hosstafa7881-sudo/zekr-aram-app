# پیشرفت فاز اول Capacitor (تبدیل به اپ اندرویدی) — «ذکرآرام»

وضعیت‌ها: ⬜ انجام‌نشده · 🟡 در حال انجام / منتظر کاربر · ✅ انجام‌شده

## موارد پرامپت

| # | مورد | وضعیت |
|---|---|---|
| ۱ | نصب Capacitor (`core`/`cli`/`android`)، `cap init`، `cap add android` | ✅ انجام‌شده |
| ۲ | آیکون برنامه (تولید همه‌ی سایزها + adaptive + legacy) و پیش‌نمایش سه ماسک | 🟡 خط تولید کامل و تست‌شده — منتظر تصمیم کاربر درباره‌ی تصویر |
| ۳ | اسپلش‌اسکرین (`@capacitor/splash-screen`) با پس‌زمینه‌ی پالت پیش‌فرض | ✅ انجام‌شده |
| ۴ | تنظیمات `capacitor.config.ts` | ✅ انجام‌شده |
| ۵ | بیلد و سینک اولیه (`npm run build` + `cap sync android` + تلاش برای APK دیباگ) | 🟡 build و sync ✅ — ساخت APK در سندباکس ممکن نیست (شرح پایین) |
| ۶ | `versionCode` / `versionName` اندروید و توضیح تفاوتشان با «شماره‌ی نسخه»ی داخل اپ | ✅ انجام‌شده (`versionCode 1` · `versionName "1.0.0"`) |

## دستور نهایی

| # | مورد | وضعیت |
|---|---|---|
| ۷ | اطمینان از این‌که هیچ متن/رنگ/منطقی در وب‌اپ تغییر نکرده | ✅ انجام‌شده (`git diff` روی `src/`، `index.html`، `public/` کاملاً خالی) |
| ۸ | گزارش تفکیکی (اسکرین‌شات ماسک‌ها، اسپلش، نتیجه‌ی بیلد، توضیح versionCode) | ✅ انجام‌شده |
| ۹ | خلاصه‌ی غیرفنی + قدم بعدی کاربر | ✅ انجام‌شده |
| ۱۰ | ساخت Pull Request جدید | ✅ انجام‌شده — PR #10 |

## ابهام‌هایی که باید از کاربر پرسیده شود

| موضوع | وضعیت |
|---|---|
| فایل واقعی تصویر آیکون (ضمیمه‌ی پیام در محیط اجرا موجود نیست) | ⬜ منتظر پاسخ |
| تأیید نهایی `com.zekraram.app` به‌عنوان App ID (بعد از انتشار قابل تغییر نیست) | ⬜ منتظر پاسخ |

---

## وضعیت ساخت APK در این محیط

`./gradlew assembleDebug` اجرا شد و با خطای شبکه شکست خورد — نه خطای کد:

```
Could not GET 'https://dl.google.com/dl/android/maven2/.../gradle-8.13.0.pom'.
Received status code 403 from server: Forbidden
```

سیاست شبکه‌ی این محیط اجرا، دسترسی به مخزن Maven گوگل (`dl.google.com`) را
می‌بندد، پس نه Android SDK قابل نصب است و نه وابستگی‌های اندروید قابل دانلود.
این مرحله روی کامپیوتر کاربر با **Android Studio** انجام می‌شود.

## خط تولید آیکون (قابل اجرای مجدد با یک دستور)

```
npm run icons:android     # ساخت منابع + تولید همه‌ی سایزها + اصلاح XML آیکون تطبیقی
npm run preview:icons     # پیش‌نمایش چهار ماسک
npm run preview:splash    # پیش‌نمایش اسپلش
npm run android:sync      # build وب + کپی داخل پروژه‌ی اندروید
```

برای عوض کردن تصویر آیکون کافی است یک PNG مربعی (۱۰۲۴×۱۰۲۴ یا بزرگ‌تر) در
`resources/icon-source.png` گذاشته و `npm run icons:android` اجرا شود.

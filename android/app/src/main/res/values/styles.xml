<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- مورد ۷ — a solid fill instead of @null, so nothing shows through
             as white or black while the WebView is still painting. -->
        <item name="android:background">@color/appBackground</item>
    </style>


    <!--
      دور هشتم / مورد ۷ — the splash screen is gone.

      There is no @drawable/splash any more: the launch window is a flat fill
      of the app's own background colour, so the app goes straight to the home
      page with no logo and no wait, and without the white/black flash that
      removing a splash usually leaves behind.

      Android 12 and newer ALWAYS draw a brief system splash of the launcher
      icon at every cold start — that belongs to the OS and no app can switch
      it off. It is made as plain as possible here and handed straight over to
      the normal theme.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/appBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@color/appBackground</item>
    </style>
</resources>
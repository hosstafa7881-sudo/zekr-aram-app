# پیشرفت دور هفتم — وب‌اپ «ذکرآرام»

وضعیت‌ها: ⬜ انجام‌نشده · 🟡 در حال انجام · ✅ انجام‌شده

## موارد پرامپت

| # | مورد | وضعیت |
|---|---|---|
| ۱ | باگ: کپشن (پیام همراه) عکس صفحه‌ی شمارش فرستاده نمی‌شود | ✅ انجام‌شده |
| ۲ | تست کادر پایینی (فروشگاه‌ها + QR) روی عکس کد ۱۰۰٪ با لینک آزمایشی | ✅ انجام‌شده |

### جزئیات مورد ۱

- ریشه‌یابی با Playwright: کد **همیشه** `text` را به `navigator.share` می‌داد و
  مقدارش هم خالی نبود (۲۰۶ کاراکتر، قالب کامل «ذکرهای امروز»). پس «ست نشدن» یا
  «خالی محاسبه شدن» رد شد.
- دو مسیر واقعی پیدا شد که کپشن در آن‌ها بی‌صدا گم می‌شد:
  1. `navigator.canShare` فقط با `{ files }` چک می‌شد ولی `share()` با
     `{ files, text, title }` صدا زده می‌شد؛ مرورگری که این ترکیب را رد کند
     باعث می‌شد `catch` آن را «کاربر لغو کرد» بفهمد و هیچ‌چیز فرستاده نشود.
  2. در نبود Web Share، عکس فقط دانلود می‌شد و کپشن کاملاً دور ریخته می‌شد.
- اصلاح: اعتبارسنجی دقیقاً همان payload + پله‌پله پایین آمدن
  (`files+text+title` → `files+text` → `files`) + همیشه کپی‌کردن کپشن در
  حافظه‌ی موقت به‌عنوان تور نجات + پیام کوتاه به کاربر.
- تست: `scripts/qa-v7.mjs` — ۲۷ بررسی، همه PASS.

### جزئیات مورد ۲

- با لینک آزمایشی کافه‌بازار/مایکت (`npm run qa:storelinks`): کادر سفید پایین
  کشیده می‌شود، نام فروشگاه‌ها درست است، جمله‌ی «یا کد کنار رو با دوربین گوشی
  اسکن کن» هست، و QR با `jsQR` واقعاً خوانده شد و دقیقاً به لینک کافه‌بازار
  اشاره می‌کند. ۹/۹ بررسی PASS. باگی پیدا نشد.
- `src/config/storeLinks.ts` دوباره خالی است (اسکریپت خودش برمی‌گرداند).
- اسکرین‌شات‌ها: `docs/v7/`

## دستور نهایی

| # | مورد | وضعیت |
|---|---|---|
| ۳ | `build` و `type-check` تمیز پاس شوند | ✅ انجام‌شده |
| ۴ | تست Playwright در چند اندازه‌ی موبایل + تبلت | ✅ انجام‌شده (۳۶۰ · ۳۹۰ · ۴۱۲ · تبلت ۸۰۰) |
| ۵ | بالا بردن شماره‌ی نسخه در `src/lib/version.ts` و `public/version.json` | ✅ انجام‌شده (۶ → ۷) |
| ۶ | گزارش تفکیکی برای هر دو مورد (چت + توضیحات PR) | ✅ انجام‌شده |
| ۷ | خلاصه‌ی غیرفنی + چک‌لیست تست دستی گوشی | ✅ انجام‌شده |
| ۸ | ساخت Pull Request جدید | 🟡 در حال انجام |

## نیاز به تست دستی کاربر روی گوشی

ارسال واقعی `navigator.share` در سندباکس قابل اجرا نیست (هدلس کروم پنجره‌ی
اشتراک‌گذاری اندروید را ندارد). بنابراین مسیرها با شبیه‌سازی رفتار گوشی تست
شده‌اند، نه با پنجره‌ی واقعی اندروید. چک‌لیست دستی در توضیحات PR آمده است.

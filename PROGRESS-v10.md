# پیشرفت دور دهم (v10)

وضعیت‌ها: ⬜ انجام‌نشده · 🟡 در حال انجام · ✅ انجام‌شده

## الف) اشتراک‌گذاری و تصویرها

| # | مورد | وضعیت |
|---|---|---|
| ۱ | پیام «متن هم کپی شد» قبل از باز شدن پنجره‌ی اشتراک‌گذاری نشان داده شود، نه بعد از برگشتن کاربر | ✅ |
| ۲ | لینک‌های فروشگاه در کپشن: خط خالی قبل از بلوک، و لینک روی خط جداگانه‌ی خودش | ✅ |
| ۳ | خط «دانلود از …» در تصویر صفحه‌ی شمارش فاصله‌ی یک‌خطی بگیرد | ✅ |
| ۴ | تصویر استوری: متن کیوآر دو خطی (خط دوم بولد) + برچسب «کیوآر کد» زیر کد | ✅ |

## ب) تقویم

| # | مورد | وضعیت |
|---|---|---|
| ۵ | «آغاز امامت حضرت ولیعصر (عج)» از ۳۱ مرداد به ۳۰ مرداد (۹ → ۸ ربیع‌الاول) | ✅ |

## پ) یادآوری روزانه — یک ساعت دیر می‌آمد

| # | مورد | وضعیت |
|---|---|---|
| ۶ | رفع اختلاف منطقه‌ی زمانی: ساعت دیواری دیگر در جاوااسکریپت به لحظه تبدیل نشود | ✅ |
| ۷ | پلاگین بومی `ReminderAlarmPlugin` با `setAlarmClock()` و محاسبه‌ی ساعت در اندروید | ✅ |
| ۸ | دفتر ثبت رسیدن اعلان‌ها + نمایش سه مورد آخر در کارت وضعیت | ✅ |
| ۹ | دکمه‌ی «یادآوری آزمایشی برای ۲ دقیقه بعد» | ✅ |
| ۱۰ | تصحیح متن کارت وضعیت (خواندن از آلارم واقعی به‌جای حافظه‌ی پلاگین) | ✅ |

## ت) ستاره و پاک‌کردن داده‌ها

| # | مورد | وضعیت |
|---|---|---|
| ۱۱ | پیام «اولین ستاره» بعد از پاک‌کردن داده‌ها نشان داده شود | ⬜ |
| ۱۲ | بازبینی کامل «پاک کردن کامل داده‌ها» — شش کلید دیگر پاک شوند، سه کلید اشتراک عمداً بمانند | ⬜ |

## ث) آماده‌سازی کافه‌بازار

| # | مورد | وضعیت |
|---|---|---|
| ۱۳ | فعلاً فقط کافه‌بازار (آدرس مایکت خالی شود، ردیفش بماند) | ✅ |
| ۱۴ | خاموش‌کردن «کد تخفیف ۱۰۰ درصدی» برای ثبت اول | ⬜ |

## چ) سازگاری با دستگاه‌ها

| # | مورد | وضعیت |
|---|---|---|
| ۱۵ | `scripts/qa-devices.mjs` — همه‌ی صفحه‌ها در ۱۰ اندازه، روز و شب، بدون سرریز | ⬜ |

## ج) کارهای پایانی

| # | مورد | وضعیت |
|---|---|---|
| د۱ | `build` و `type-check` تمیز | ⬜ |
| د۲ | کل مجموعه‌ی آزمون‌ها سبز + `qa-v10` تازه | ⬜ |
| د۳ | نسخه ۱۰ (`APP_VERSION` + `version.json` + versionCode 4 + versionName 1.3.0) | ⬜ |
| د۴ | به‌روزرسانی `SKILL.md` و `MESSAGES-INVENTORY.md` | ⬜ |
| د۵ | نشان‌دادن تصویرهای واقعی به کاربر قبل از پایان | ⬜ |
| د۶ | APK دیباگ + گزارش تفکیکی + چک‌لیست تست دستی | ⬜ |

## خارج از این دور

- **تبلیغات تپسل** — کاربر مشخصات بنر و پاپ‌آپ را بعداً می‌فرستد. این دور هیچ SDK
  تبلیغاتی اضافه نمی‌شود؛ `AdPlaceholder` موجود سر جایش می‌ماند.
- **جواب پشتیبانی کافه‌بازار** درباره‌ی سیستم کد تخفیف — تیکت ۳۵۰۲۸۵۲ هنوز بی‌جواب.
  با مورد ۱۴ انتشار معطل آن نمی‌ماند.

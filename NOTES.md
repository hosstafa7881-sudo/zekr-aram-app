# یادداشت‌های فنی پروژه «ذکرآرام»

این فایل خلاصه‌ای از تصمیمات معماری، محدودیت‌های فعلی و قدم‌های بعدی پروژه است.

## معماری کلی

- React 19 + TypeScript + Vite + Tailwind CSS 4، بدون هیچ سرور بک‌اندی — همه‌چیز سمت کاربر (client-side) اجرا می‌شود.
- ساختار فولدربندی بر پایه‌ی `features/`: هر قابلیت بزرگ یک فولدر مستقل زیر `src/features/` دارد (`counter`, `dhikrs`, `history`, `settings`, `home`, `notebook`, `gamification`, `subscription`).
- ذخیره‌سازی در `localStorage` با پشتیبان همزمان در `IndexedDB` (بدون تغییر نسبت به کد پایه).
- تاریخچه‌ی روزانه (`dailyLogs`) دیگر محدود به ۹۰ روز نیست و به‌صورت کامل و نامحدود نگه داشته می‌شود.

## سیستم قفل/اشتراک (Paywall)

- **از دور پنجم:** کد تخفیف ۱۰۰٪ دیگر یک دوره‌ی «Pro موقت» موازی نمی‌سازد. به‌جایش، **تاریخ پایان دسترسی رایگان** را ۳۰ روز جلو می‌برد (`zikraram_free_extension_until_v1` در `subscription.ts`). در نتیجه همه‌ی قفل‌ها، برچسب‌ها و هشدار «۵ روز مانده» از همان `useTrialGate` مشترک می‌آیند و هیچ منطق موازی دومی وجود ندارد. این تاریخ و تاریخ آخرین استفاده از کد، در فایل پشتیبان ذخیره و بازیابی می‌شوند.

- منطق در `src/lib/subscription.ts`: تاریخ اولین اجرا در `localStorage` ذخیره می‌شود و ۳۰ روز رایگان از آن تاریخ محاسبه می‌شود.
- گیت‌کردن قابلیت‌های قفل‌شونده از طریق `FeatureGateContext` (`src/lib/FeatureGateContext.tsx`) و تابع `guard(featureId, onAllowed)` که در سراسر اپ به کامپوننت‌های مربوطه پاس داده می‌شود.
- فلگ `isProUser` در تنظیمات کاربر ذخیره می‌شود و **به هیچ درگاه پرداخت واقعی وصل نیست** — دکمه‌ی «تهیه اشتراک ماهانه» در حال حاضر فقط این فلگ را به‌صورت محلی فعال می‌کند (شبیه‌سازی خرید).
- **قدم بعدی:** اتصال به یک درگاه پرداخت واقعی ایرانی (مثل زرین‌پال یا آیدی‌پی) و بازنویسی `handleSubscribe` در `PaywallView.tsx` برای فراخوانی API واقعی پس از پرداخت موفق.
- `UserSettings.proGrantExpiresAt`: یک اشتراک Pro **موقت** (فعلاً فقط برای تخفیف ۱۰۰٪ اشتراک‌گذاری) که بعد از ۳۰ روز خودکار به `isProUser: false` برمی‌گردد — دقیقاً مثل تمام شدن دوره‌ی آزمایشی. یک `useEffect` در `App.tsx` هر دقیقه این تاریخ را چک می‌کند. خرید واقعی/شبیه‌سازی‌شده (`handleSubscribe`) همیشه این فیلد را `null` می‌کند تا `isProUser` دائمی بماند.

## سیستم‌های تخفیف (بدون اتصال به سرور)

- منطق مشترک در `src/lib/discounts.ts`؛ هیچ‌کدام به ستاره/مدال مادام‌العمر (`gamification.ts`) دست نمی‌زنند.
- **سه SKU نمایشی برای اشتراک ماهانه** (`MONTHLY_PRICING_SKUS`): قیمت کامل، ۳۰٪ تخفیف، ۵۰٪ تخفیف — فقط شناسه‌های نمایشی هستند (هنوز به کافه‌بازار/مایکت/گوگل‌پلی وصل نیستند)؛ انتخاب SKU سمت کلاینت و بر پایه‌ی کد تخفیف فعال کاربر انجام می‌شود.
- **تخفیف بر پایه‌ی تعداد ذکر (۳۰٪/۵۰٪)**: چرخه‌ی ۳۰ روزه‌ی چرخشی مستقل از دوره‌ی آزمایشی (لنگرگاه جدا در `zikraram_discount_cycle_start_v1`)، پیگیری در `useCountDiscountEvents`. **نکته‌ی مهم:** چون `dailyLogs` فقط دقت روزانه دارند (نه دقیقه/ثانیه)، مرزهای چرخه به نیمه‌شب گرد می‌شوند (`toMidnight`) وگرنه ذکرهای گفته‌شده در همان روزی که چرخه شروع می‌شود، به‌اشتباه از محاسبه حذف می‌شدند.
- **تخفیف اشتراک‌گذاری (۱۰۰٪، هر ۱۸۰ روز)**: کاملاً مبتنی بر اعتماد، بدون هیچ بررسی سمت سرور؛ در `useReferralDiscount`.
- کامپوننت‌های UI در `src/features/discounts/`.

## اعلانات و یادآوری

- یادآوری روزانه (`src/lib/notifications.ts`) از `Notification API` استاندارد مرورگر استفاده می‌کند و هر ۶۰ ثانیه در حین باز بودن اپ بررسی می‌شود.
- **محدودیت مهم:** چون این یک PWA است نه اپ Native، این یادآوری فقط زمانی کار می‌کند که اپ باز یا اخیراً باز بوده باشد؛ اعلان واقعی در پس‌زمینه (وقتی اپ کاملاً بسته است) با وب استاندارد ممکن نیست.
- **قدم بعدی (فاز Capacitor):** جایگزینی این ماژول با پلاگین `@capacitor/local-notifications` برای زمان‌بندی اعلان‌های قابل‌اعتماد سطح سیستم‌عامل، حتی وقتی اپ بسته است.

## تقویم مناسبت‌های مذهبی

- تبدیل تاریخ میلادی به قمری از طریق `Intl.DateTimeFormat('en-US-u-ca-islamic')` انجام می‌شود (`src/utils/hijri.ts`) — همان روشی که تبدیل شمسی در `persian.ts` استفاده می‌کند.
- این تبدیل تقریبی است (احتمال اختلاف یک روزه حول مرزهای رؤیت هلال) و فقط برای نمایش مناسبت‌های نزدیک مناسب است، نه برای احکام شرعی دقیق.
- داده‌های مناسبت‌ها در `src/data/religiousOccasions.ts` به‌صورت آرایه‌ی ساده (بدون منطق) نگه داشته شده تا به‌راحتی قابل ویرایش/افزودن باشد.

## تقویم شمسی جست‌وجوی تاریخچه

- تبدیل شمسی↔میلادی در `src/utils/jalali.ts` به‌جای پیاده‌سازی دستی جدول جهش‌های کبیسه‌ی جلالی، از یک جست‌وجوی محلی حول یک تخمین اولیه استفاده می‌کند که نتیجه‌اش را با همان تبدیل میلادی→شمسی قابل‌اعتماد `persian.ts` تطبیق می‌دهد. این روش از باگ‌های ظریف احتمالی در پیاده‌سازی دستی الگوریتم جلالی جلوگیری می‌کند.

## تبلیغات

- `src/components/AdPlaceholder.tsx` فقط یک جای‌گیر بصری است و به هیچ SDK تبلیغاتی واقعی وصل نیست. برای کاربران مشترک (`isProUser`) به‌طور کامل مخفی می‌شود.
- **قدم بعدی:** اتصال به یک SDK تبلیغاتی (مثلاً AdMob بعد از تبدیل به Capacitor).

## لینک‌های دانلود از بازارها

- `src/config/storeLinks.ts` — چون اپ هنوز منتشر نشده، همه‌ی لینک‌ها خالی هستند. هر بازاری که لینکش خالی باشد به‌صورت خودکار از UI اشتراک‌گذاری حذف می‌شود (نه دکمه‌ی خراب).
- **قدم بعدی:** پر کردن این لینک‌ها بعد از انتشار در کافه‌بازار، مایکت و گوگل‌پلی.

## ویجت صفحه‌ی اصلی اندروید

- در وب‌اپ فعلی پیاده‌سازی نشده (فنی ممکن نیست). فقط در صفحه‌ی اشتراک به‌عنوان یکی از مزایای نسخه‌ی اندرویدی ذکر شده است.
- **قدم بعدی (فاز Capacitor):** استفاده از افزونه‌ی ویجت Capacitor برای اندروید بعد از بسته‌بندی اپ.

## حالت شب/روز و رنگ‌بندی

- کل سیستم رنگ بر پایه‌ی CSS Custom Properties (`--bg`, `--surface`, `--accent`, ...) در `src/index.css` است. حالت شب/روز از طریق `data-theme` و رنگ‌بندی از طریق `data-palette` روی `<html>` اعمال می‌شود (`src/lib/theme.ts`).
- این معماری یعنی افزودن یک پالت رنگی جدید فقط نیاز به افزودن چند خط CSS دارد، بدون تغییر در کامپوننت‌ها.
- **رنگ سبز اصلی دکمه‌ها/آیکون‌ها (`--accent`) ثابت است** و مستقل از پالت پس‌زمینه و حالت شب/روز — با `--brand-h/--brand-s/--brand-l` در `index.css` تعریف شده، نه با `--hue`/`--pal-sat` که فقط پس‌زمینه را رنگ می‌زنند. پالت پس‌زمینه ۶ گزینه دارد (سبز/کرمی/طوسی/یاسی/آبی/صورتی)، بر اساس اسکرین‌شات مرجع کاربر.
- بعد از پایان دورهٔ آزمایشی، اگر کاربر پالت یا حالت شب را عوض کرده باشد، خودکار به پیش‌فرض (سبز، روز) برمی‌گردد (`useEffect` در `App.tsx`).

## ذکر چندمرحله‌ای دلخواه

- کاربر می‌تواند ذکر چندمرحله‌ای دلخواه با تعداد مرحلهٔ دلخواه بسازد (`DhikrItem.customStages` در `seedData.ts`)، دقیقاً مثل ساختار تسبیحات اربعه. منطق تشخیص مرحلهٔ جاری در `src/features/counter/customStageHelpers.ts` عمومی و مستقل از تعداد مرحله است.
- هم **ساخت** و هم **استفاده از** ذکر چندمرحله‌ای دلخواه بعد از پایان دورهٔ آزمایشی قفل می‌شود — این عمداً است تا نتوان با ساخت یک ذکر چندمرحله‌ای مشابه تسبیحات اربعه/زهرا، قفل آن دو را دور زد. کلیک روی یک ذکر چندمرحله‌ای که قبلاً ساخته شده، پیام اختصاصی (نه پیام عمومی قفل) نشان می‌دهد — `FeatureGateContext`'s `guard()` حالا یک پارامتر سوم اختیاری (`customLockedMessage`) می‌گیرد که دقیقاً برای همین مورد اضافه شد.

## پیام‌ها

- `src/lib/messages.ts` مرجع مرکزی تمام پیام‌های «دیکشنری» محصول است (بعضی مستقیماً همان‌جا تعریف شده‌اند، بعضی از ماژول طبیعی خودشان — مثل `subscription.ts` یا `gamification.ts` — دوباره export شده‌اند تا یک منبع واحد برای ممیزی متن باقی بماند).
- `MESSAGES-INVENTORY.md` در ریشه‌ی ریپو، فهرست کامل و انسانی‌خوانِ همه‌ی متن‌های قابل‌مشاهده برای کاربر است (کجا و در چه شرایطی دیده می‌شوند). با هر تغییر متن، این فایل هم باید به‌روز شود.

## شماره‌ی نسخه و به‌روزرسانی خودکار

- `src/lib/version.ts` (`APP_VERSION`) و `public/version.json` **باید همیشه یک عدد یکسان داشته باشند**؛ در هر دور، هر دو یکی بالا می‌روند.
- `src/lib/useAppUpdate.ts` هر ۵ دقیقه (و هر بار که اپ به پیش‌زمینه برمی‌گردد) `version.json` را با `cache: 'no-store'` می‌خواند. اگر عدد سرور بزرگ‌تر بود، اول خود `index.html` را با `cache: 'reload'` تازه می‌کند و بعد `location.reload()` می‌زند.
- چون بیلد با `vite-plugin-singlefile` یک فایل `index.html` تکی است، هیچ نام فایل هش‌داری وجود ندارد که خودش cache-busting کند؛ به همین دلیل این روش انتخاب شد (نه Service Worker، که خطر «گیر کردن روی SW قدیمی» دارد).
- یک نگهبان در `sessionStorage` هست تا اگر میزبان همچنان HTML قدیمی بدهد، اپ در حلقه‌ی بارگذاری مکرر نیفتد.

## تصویرهای تولیدشده در لحظه

- `src/lib/canvasKit.ts` ابزارهای مشترک رسم روی canvas (رنگ‌های تم، متن راست‌به‌چپ با فونت Vazirmatn، کادر گوشه‌گرد، شکستن خط).
  - **نکته‌ی مهم:** اندازه‌ی فونت را باید با regex از روی `...px` بیرون کشید، نه `parseInt` — چون در قالب کوتاه CSS اول **وزن** فونت می‌آید (`800 48px ...`) و `parseInt` عدد ۸۰۰ را برمی‌دارد. این باگ یک‌بار ارتفاع خط را ۲۰ برابر کرد.
- `src/lib/counterShareImage.ts` تصویر صفحه‌ی شمارش (مورد ۶ دور پنجم).
- `src/lib/storyImage.ts` تصویر آماده‌ی استوری ۱۰۸۰×۱۹۲۰ (مورد ۱۹). بخش ثابتش (`public/story-collage.png`) با `npm run build:story-collage` از روی اسکرین‌شات‌های واقعی خود برنامه ساخته می‌شود و کاربر می‌تواند این فایل را با طرح دلخواه خودش عوض کند.
- `src/lib/qrCode.ts` QR واقعی با کتابخانه‌ی `qrcode-generator` (بدون نیاز به اینترنت).

## تست خودکار

- اسکریپت‌های Playwright زیر `scripts/` هستند و همه با `npm run qa` اجرا می‌شوند:
  `qa:layout` (چیدمان در ۴ اندازه + تور)، `qa:flows` و `qa:flows2` (سناریوهای رفتاری)،
  `qa:occasions` (متن اعلان مناسبت‌ها با جابه‌جا کردن ساعت مرورگر)، `qa:misc`
  (به‌روزرسانی خودکار و پیش‌فرض‌ها). `qa:storelinks` جداگانه اجرا می‌شود چون
  `storeLinks.ts` را موقتاً پر می‌کند، بیلد می‌گیرد، QR را با `jsQR` می‌خواند و بعد فایل را برمی‌گرداند.
- در سندباکس، مرورگر پین‌شده‌ی سیستم استفاده می‌شود (`scripts/lib/browser.mjs`)، نه دانلود Playwright.

## آموزش اولیه (Onboarding)

- تور آموزشی (`src/components/OnboardingTour.tsx`) از تکنیک استاندارد CSS (`box-shadow` برای افکت نورافکن + `getBoundingClientRect` برای موقعیت‌یابی) استفاده می‌کند، **نه** از `clip-path` پیچیده یا canvas — تا با مشکل شناخته‌شده‌ی نسخه‌ی قبلی (مربع سفید خالی روی برخی گوشی‌های اندرویدی مثل شیائومی) دوباره روبه‌رو نشویم.
- **باگ ریشه‌ای پیدا و رفع‌شده (تست کامل با Playwright):** کلاس `.onboarding-spotlight` در `index.css` یک `position` تعریف نمی‌کند دیگر — قبلاً `position: relative` داشت که چون این فایل بعد از `@import "tailwindcss"` بارگذاری می‌شود، با specificity برابر روی کلاس Tailwind `absolute` (که در JSX روی همین المان است) غالب می‌شد. نتیجه: مربع هایلایت واقعاً `position: relative` رندر می‌شد، و چون صفحه RTL است، موقعیت افقی پیش‌فرض (static position) این نوع باکس به‌جای گوشه‌ی چپ صفحه، به سمت راست کانتینر می‌چسبید — دقیقاً همان چیزی که در تست موبایل «همه‌ی مراحل موقعیت اشتباه دارن» گزارش شده بود (top/width/height درست بودند چون به این مسئله وابسته نیستند، فقط left کاملاً اشتباه بود). این یک باگ واحد بود که هر پنج مرحله را همزمان خراب می‌کرد.

## قدم‌های بعدی خلاصه‌شده

۱. اتصال Capacitor برای تبدیل به اپ اندرویدی (APK) برای کافه‌بازار/مایکت/گوگل‌پلی.
۲. اتصال یک درگاه پرداخت واقعی برای اشتراک ماهانه.
۳. اتصال یک SDK تبلیغاتی واقعی.
۴. جایگزینی یادآوری مرورگری با Capacitor Local Notifications.
۵. افزودن ویجت صفحه‌ی اصلی اندروید با Capacitor Widget Plugin.
۶. پر کردن لینک‌های دانلود از بازارها پس از انتشار.
